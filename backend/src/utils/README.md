# utils/

Chhote, pure helper functions — koi business logic nahi.

`logger.ts`, `slugify.ts`, `pagination.ts`, `money.ts` (cents <-> display),
`asyncHandler.ts` (async controllers ki errors pakarne ke liye).
