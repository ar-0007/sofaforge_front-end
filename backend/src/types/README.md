# types/

Backend-only TypeScript types — Express ke `Request` me `user` add karna
(`express.d.ts`), internal DTOs waghera.

Jo types frontend ko bhi chahiye wo yahan **nahi**, root ke `shared/` me jayenge.
