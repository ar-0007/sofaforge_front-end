import type { Metadata } from "next";
import Page from "@/views/CustomStudio";

export const metadata: Metadata = {
  title: "Custom Studio",
  description: "Configure your own sofa — choose the silhouette, fabric, colour and size in the Sofa Co. Custom Studio.",
  alternates: { canonical: "/custom-studio" },
};

export default Page;
