import type { Metadata } from "next";
import Page from "@/views/ComponentShowcase";

export const metadata: Metadata = {
  title: "Component showcase",
  description: "Internal component showcase.",
  alternates: { canonical: "/components" },
};

export default Page;
